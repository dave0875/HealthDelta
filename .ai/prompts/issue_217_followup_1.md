Issue: #217 follow-up 1

This follow-up does NOT change issue scope or acceptance criteria.

Purpose
- Record the governance-only continuation after CI failed because the original `#217` implementation commit omitted the required `Issue: #217` commit-message footer.

Execution constraints
- Do not change `#217` scope, acceptance criteria, or the technical fix.
- Add only the minimal audit artifacts needed to capture the CI rerun and issue closure.
