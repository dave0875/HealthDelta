# Review — Issue 187 (2026-03-08)

Issue: https://github.com/dave0875/HealthDelta/issues/187

Reviewer model
- `codex/gpt-5`

Change summary
- Completed the explicit Encounter NDJSON field contract for hospital FHIR exports.
- Extended DuckDB encounter ingestion/schema to preserve `encounter_id`, subject reference, and encounter period bounds.
- Kept reporting compatible with the richer Encounter schema and verified Encounter counts by source remain present.
- Updated `docs/runbook_ndjson.md` and `docs/runbook_duckdb.md` to document the Encounter contract.

Findings
- Acceptance criteria: Met locally. Encounter rows now include the required identifiers/reference/period fields, the DuckDB `encounters` table preserves them, and reporting continues to include encounter counts by source.
- Scope control: Good. This stays within Encounter export/loading/reporting support and does not introduce unrelated mapping logic.
- Determinism/privacy: Good. Added fields are stable structured identifiers/timestamps only; no patient names, DOB, or free text were introduced.

Risks / edge cases
- CDA encounter-like rows remain separate as `CDAEncounter` and do not receive the FHIR-specific `encounter_id` contract. That is consistent with the issue scope, but downstream consumers should not assume every row in `encounters.ndjson` has the same origin semantics.
