# Review — Issue 191 (2026-03-08)

Issue: https://github.com/dave0875/HealthDelta/issues/191

Reviewer model
- `codex/gpt-5`

Change summary
- Added new canonical NDJSON streams for `Goal` and `CarePlan`.
- Implemented stable Goal/CarePlan IDs, status/intent/date fields, and CarePlan goal linkage via `goal_ids`.
- Added `goals.schema.json` and `careplans.schema.json` with validator coverage.
- Updated `docs/runbook_ndjson.md` to document the new streams and field contracts.

Findings
- Acceptance criteria: Met locally. CarePlan and Goal rows are emitted with stable identifiers, subject references, status/intent/dates, and CarePlan goal links.
- Scope control: Good. This slice adds only the export and validation surface required by the issue; it does not attempt downstream analytics or UI work.
- Determinism/privacy: Good. The new rows are structured, stable, and contain no free-text patient identifiers or attachments.

Risks / edge cases
- CarePlan and Goal are currently export/validation only; they are not yet loaded into DuckDB or reporting. That is consistent with this issue’s scope, but any downstream use will need a follow-up issue.
