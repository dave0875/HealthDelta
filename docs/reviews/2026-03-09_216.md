# Review — Issue 216 (2026-03-09)

Issue: https://github.com/dave0875/HealthDelta/issues/216

Reviewer model
- `codex/gpt-5`

Change summary
- Added the first manual iOS export path via `Export Now` on the dashboard.
- Introduced a testable manual export coordinator that requests HealthKit read authorization and runs the existing incremental exporter.
- Added the required Health privacy usage string and HealthKit entitlement to the generated iOS app configuration.
- Updated `docs/runbook_ios_export.md` to document the manual export flow.

Findings
- Acceptance criteria: Met with physical-device proof. Tapping `Export Now` on the connected iPhone completed a local export and replaced `No sync data yet` with sync details showing `232,236` observations / rows, `79.3 MB`, and `Anchors active (1 files)`.
- Test coverage: Good. Added deterministic unit coverage for export orchestration and dashboard refresh/error behavior; final simulator run passed with 21 tests and 0 failures.
- Scope control: Good. The change adds only the first manual local export flow; it does not add ORIN/network ingestion into the iPhone app.

Risks / edge cases
- Insights remain empty until operator-side outputs such as `note/doctor_note.md` and `reports/summary.md` exist in the app sandbox. That matches the current architecture but may confuse first-run users until a later issue clarifies or expands the UX.
