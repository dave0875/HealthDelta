# Review — Issue 189 (2026-03-08)

Issue: https://github.com/dave0875/HealthDelta/issues/189

Reviewer model
- `codex/gpt-5`

Change summary
- Completed the explicit DiagnosticReport NDJSON field contract with identifier, subject, timing, and coding metadata fields.
- Tightened `schemas/ndjson/v2/diagnostic_reports.schema.json` so validation now enforces required DiagnosticReport-specific fields.
- Added validator coverage that fails when DiagnosticReport rows only satisfy the generic base schema.
- Updated `docs/runbook_ndjson.md` to document the DiagnosticReport contract.

Findings
- Acceptance criteria: Met locally. DiagnosticReport rows now include stable IDs, dates, codes, and subject references, and NDJSON validation enforces the required report contract.
- Scope control: Good. The change stays within DiagnosticReport export and validation; it does not introduce narrative extraction or non-share-safe payload handling.
- Determinism/privacy: Good. Exported report metadata remains structured and share-safe, and validation operates on the canonical row contract only.

Risks / edge cases
- The schema currently requires `status` but leaves code/timing fields optional to accommodate sparse share-safe fixtures. If future governance needs stronger required-field enforcement, that should be a follow-up issue with explicit fixture coverage.
