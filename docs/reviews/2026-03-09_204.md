# Review — Issue 204 (2026-03-09)

Issue: https://github.com/dave0875/HealthDelta/issues/204

Reviewer model
- `codex/gpt-5`

Change summary
- Added a new canonical `binaries.ndjson` stream for FHIR Binary resources.
- Preserved share-safe attachment metadata in `DocumentReference` and `DiagnosticReport` rows through structured `binary_id` references.
- Added `binaries.schema.json` and validator coverage.
- Updated `docs/runbook_ndjson.md` to document Binary handling and attachment redaction rules.

Findings
- Acceptance criteria: Met locally. Binary rows emit stable metadata and attachment references resolve through `binary_id` without exposing raw payloads.
- Scope control: Good. This slice stays within export and validation and does not expand into raw file packaging or rendering.
- Determinism/privacy: Good. Raw `data` is excluded from canonical NDJSON while size/hash metadata and linkage remain stable and share-safe.

Risks / edge cases
- FHIR Binary resources without inline `data` still emit `data_present=false`, but `content_size_bytes` and `content_sha256` stay absent. If future workflows require validating remote Binary fetches, that should be a separate issue.
