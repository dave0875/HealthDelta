# Review — Issue 184 (2026-03-08)

Issue: https://github.com/dave0875/HealthDelta/issues/184

Reviewer model
- `codex/gpt-5`

Change summary
- Completed the Condition NDJSON field contract with code/status/onset extraction.
- Added deterministic share-safe warning-summary output for missing Condition fields.
- Expanded exporter tests to cover one complete Condition and one missing-field Condition.
- Updated `docs/runbook_ndjson.md` to document the Condition mapping fields and warnings.

Findings
- Acceptance criteria: Met locally. Condition rows now include the required canonical fields, deterministic `record_key`/`canonical_person_id`, and a share-safe warning summary for missing required fields.
- Scope control: Good. This stays within Condition mapping and does not alter unrelated resource contracts.
- Determinism/privacy: Good. Missing-field handling is null/empty plus aggregate warning counts only; no raw payload data is exposed.

Risks / edge cases
- The warning summary currently writes to stderr during NDJSON export; if future operators need persisted warning artifacts, that should be handled in a separate issue instead of changing this contract implicitly.
