# Review — Issue 217 (2026-03-10)

Issue: https://github.com/dave0875/HealthDelta/issues/217

Reviewer model
- `codex/gpt-5`

Change summary
- Reworked fresh iOS observation loads in `healthdelta/duckdb_tools.py` to use DuckDB native NDJSON ingestion instead of a slow Python row-by-row path.
- Added deterministic dedupe for duplicate iOS observation `record_key` rows during fresh import.
- Added regression coverage for large batched iOS loads and documented the validated `iPhone -> Mac -> ORIN` operator path.

Findings
- Acceptance criteria: Met. On ORIN, the copied iOS export at `/home/dbarker/ios_exports/run_20260310_020742` now builds to DuckDB in about `6.9s` and share-safe reports build in about `0.45s`.
- Correctness: Good. The import still preserves append-safe behavior for existing DBs, and fresh iOS loads now deterministically remove duplicate rows that share a `record_key` within the same `observations.ndjson` file.
- Test coverage: Good. `tests/test_duckdb_ios.py`, `tests/test_reports_ios.py`, `tests/test_duckdb.py`, and `tests/test_reports.py` all passed locally in the project `.venv`.

Risks / edge cases
- The deployed ORIN backend container image still lacks the `duckdb` Python package, so the validated path currently depends on running the repo code on the ORIN host (`PYTHONPATH=$HOME/HealthDelta-temp`) rather than through the deployed backend container.
