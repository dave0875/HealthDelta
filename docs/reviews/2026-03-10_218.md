# Review — Issue 218 (2026-03-10)

Issue: https://github.com/dave0875/HealthDelta/issues/218

Reviewer model
- `codex/gpt-5`

Change summary
- Strengthened the iOS HealthKit observation identity contract by exporting `source_id` from the sample UUID and deriving `record_key` from that stable identity.
- Updated downstream DuckDB loading to preserve iOS `source_id`.
- Added regression tests for distinct-sample identity and documented the new contract in the iOS/DuckDB runbooks.

Findings
- Acceptance criteria: Met locally. Two distinct HealthKit samples with identical visible fields now produce different `record_key` values in the iOS exporter tests.
- iOS proof: Good. Mac simulator suite passed with `22` tests and `0` failures after the identity change.
- Python proof: Good. iOS DuckDB/report tests passed locally and confirm the stronger iOS schema remains ingestible.

Risks / edge cases
- Previously exported iOS runs keep their older weaker `record_key` values. The strengthened identity contract applies to newly produced iOS exports going forward.
