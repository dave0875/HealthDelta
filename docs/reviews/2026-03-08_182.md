# Review — Issue 182 (2026-03-08)

Issue: https://github.com/dave0875/HealthDelta/issues/182

Reviewer model
- `codex/gpt-5`

Change summary
- Added `profile/` to the share-bundle allowlist so `clinical_inventory.json` is bundled alongside report coverage artifacts.
- Extended share-bundle tests to assert inclusion of `coverage.json`, `coverage.md`, and `clinical_inventory.json`, including the zero-count case.
- Updated `docs/runbook_share_bundle.md` to document the hospital-record evidence artifacts now included in bundles.

Findings
- Acceptance criteria: Met locally. Share bundles now include the requested hospital-record evidence artifacts, and the bundle manifest continues to hash/list them.
- Determinism/privacy: Good. The new inclusion path relies on the existing stable tar/manifest logic and carries only share-safe derived artifacts.
- Scope control: Good. No raw exports or new bundle format changes were introduced.

Risks / edge cases
- `clinical_inventory.json` inclusion depends on the run tree containing a `profile/` directory; workflows that never materialize the profile artifact will still need to add it before bundling.
