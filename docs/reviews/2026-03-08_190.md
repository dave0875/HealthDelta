# Review — Issue 190 (2026-03-08)

Issue: https://github.com/dave0875/HealthDelta/issues/190

Reviewer model
- `codex/gpt-5`

Change summary
- Completed the explicit DocumentReference NDJSON field contract with identifier, subject, and type metadata fields.
- Added share-safe attachment metadata export while excluding raw payload fields like `data` and `url`.
- Tightened `schemas/ndjson/v2/documents.schema.json` so validation now enforces the DocumentReference-specific contract.
- Updated `docs/runbook_ndjson.md` to document the DocumentReference contract and attachment redaction behavior.

Findings
- Acceptance criteria: Met locally. DocumentReference rows now include stable IDs, dates, type codes, and subject references, and attachment export remains limited to share-safe metadata.
- Scope control: Good. The change stays within DocumentReference export and validation; it does not introduce binary extraction or storage.
- Determinism/privacy: Good. Attachment metadata is sorted and share-safe, while raw payload fields are excluded from canonical NDJSON.

Risks / edge cases
- The current share-safe attachment summary preserves `hash` when available. If downstream policy later treats hashes as too identifying for certain workflows, that would need an explicit governance follow-up rather than a silent contract change.
