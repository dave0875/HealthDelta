# iOS App (Placeholder)

The iOS app will live here.

Notes:
- iOS builds/signing occur on macOS only.
- Linux development should SSH to macOS to run Xcode commands; document the workflow when implemented.

